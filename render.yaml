services:
  - type: web
    name: quiz-backend
    runtime: node
    region: oregon
    plan: free
    rootDir: quiz-backend
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
